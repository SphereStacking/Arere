/**
 * Prompt API types
 * Re-exports types from action/types.ts for convenience
 */

export type {
  PromptAPI,
  TextOptions,
  NumberOptions,
  PasswordOptions,
  SelectOptions,
  ConfirmOptions,
  MultiSelectOptions,
  SelectChoice,
} from '@/domain/action/types'
