---
title: settings.json
description: Application settings
---

# settings.json

Configure the application behavior.

## Basic Structure

```json
{
  "logLevel": "info",
  "locale": "en",
  "theme": {
    "primaryColor": "green"
  },
  "plugins": {
    "arere-plugin-git": true
  },
  "actionsDir": ".arere"
}
```

## Configuration Options

### logLevel

Specify the log output level.

```json
{
  "logLevel": "info"
}
```

Available values: `"trace"`, `"debug"`, `"info"`, `"warn"`, `"error"`, `"fatal"`

### locale

Specify the UI language.

```json
{
  "locale": "en"
}
```

Available values: `"ja"` (Japanese), `"en"` (English)

### theme

Specify the UI theme.

```json
{
  "theme": {
    "primaryColor": "cyan"
  }
}
```

Available primaryColor: `"green"`, `"cyan"`, `"magenta"`, `"yellow"`, `"blue"`, `"red"`, `"white"`

### ui

Customize UI behavior.

```json
{
  "ui": {
    "bookmarkIcon": "♥"
  }
}
```

#### ui.bookmarkIcon

The icon displayed next to bookmarked actions in the list.

Default: `"♥"`

Example values: `"🔖"`, `"⭐"`, `"★"`, `"♥"`, `"📌"`

#### ui.actionListFormat

Customize the display format of the action list using VSCode-style template syntax.

```json
{
  "ui": {
    "actionListFormat": "${selectIcon:width(2)}[${category:max}] ${name:max} ${description:grow} ${tags:max:dim:right} ${bookmark:width(2)}"
  }
}
```

**Available variables:**

| Variable | Description | Example |
|----------|-------------|---------|
| `${selectIcon}` | Selection indicator | `❯` or ` ` |
| `${bookmark}` | Bookmark icon | `♥` or ` ` |
| `${category}` | Category label | `git`, `Project` |
| `${name}` | Action name | `my-action` |
| `${description}` | Description | `Does something` |
| `${tags}` | Tags (with #) | `#git #dev` |
| `${plugin}` | Plugin name | `arere-plugin-git` |
| `${source}` | Source | `project`, `global`, `plugin` |

**Available modifiers:**

| Modifier | Category | Description | Example |
|----------|----------|-------------|---------|
| `:max` | Width | Align to max width of same variable | `${name:max}` |
| `:width(N)` | Width | Fixed width of N characters (truncates with `…` if exceeded) | `${bookmark:width(2)}` |
| `:left` | Alignment | Left-align text (default, can be omitted) | `${name:max:left}` |
| `:right` | Alignment | Right-align text (padding on left) | `${tags:max:right}` |
| `:center` | Alignment | Center-align text | `${name:max:center}` |
| `:grow` | Stretch | Fill remaining space (flexGrow) | `${description:grow}` |
| `:shrink` | Stretch | Truncate when space is limited | `${description:shrink}` |
| `:dim` | Style | Dim when not selected | `${tags:dim}` |
| `:primary` | Style | Use primary color | `${name:primary}` |

**Text alignment example:**

```
:right (right-align)           :left (left-align, default)    :center (center-align)
┌──────────┐                   ┌──────────┐                   ┌──────────┐
│1234567890│                   │1234567890│                   │1234567890│
│     12345│                   │12345     │                   │  12345   │
└──────────┘                   └──────────┘                   └──────────┘
  Padding on left                Padding on right               Padding on both
```

**Format examples:**

Minimal:
```json
{
  "ui": {
    "actionListFormat": "${selectIcon}${name}"
  }
}
```

No category:
```json
{
  "ui": {
    "actionListFormat": "${selectIcon:width(2)}${name:max} ${description:grow} ${bookmark:width(2)}"
  }
}
```

Right-aligned numbers:
```json
{
  "ui": {
    "actionListFormat": "${index:width(5):right}| ${name:max}"
  }
}
```

Bookmark on left:
```json
{
  "ui": {
    "actionListFormat": "${selectIcon:width(2)}${bookmark:width(2)} ${name:max} - ${description:grow}"
  }
}
```

### bookmarks

Array of bookmarked action IDs. This is automatically managed when you toggle bookmarks with the Tab key.

```json
{
  "bookmarks": [
    "local:my-action",
    "arere-plugin-git:clone-repo"
  ]
}
```

Format: `{source}:{actionName}`
- `local:actionName` - Local actions (project or global)
- `pluginName:actionName` - Plugin actions

### plugins

Specify which plugins to use.

```json
{
  "plugins": {
    "arere-plugin-git": true,
    "arere-plugin-docker": {
      "enabled": true,
      "config": {
        "defaultRegistry": "docker.io"
      }
    },
    "arere-plugin-aws": false
  }
}
```

### actionsDir

Specify the directory where actions are placed.

```json
{
  "actionsDir": "./sys-actions"
}
```

Default: `.arere`

## Merge Behavior

settings.json performs **deep merge**. Only the items specified in workspace settings are overwritten.

**User settings** (`~/.arere/settings.json`):
```json
{
  "logLevel": "debug",
  "locale": "en",
  "plugins": {
    "arere-plugin-git": true
  }
}
```

**Workspace settings** (`.arere/settings.json`):
```json
{
  "logLevel": "info",
  "plugins": {
    "arere-plugin-docker": true
  }
}
```

**Result**:
```json
{
  "logLevel": "info",           // Overwritten by workspace
  "locale": "en",               // Inherited from user settings
  "plugins": {
    "arere-plugin-git": true,   // Inherited from user settings
    "arere-plugin-docker": true // Added by workspace
  }
}
```
