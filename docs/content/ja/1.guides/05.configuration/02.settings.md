---
title: settings.json
description: アプリケーション設定
---

# settings.json

アプリケーションの動作を設定します。

## 基本構造

```json
{
  "logLevel": "info",
  "locale": "ja",
  "theme": {
    "primaryColor": "green"
  },
  "plugins": {
    "arere-plugin-git": true
  },
  "actionsDir": ".arere"
}
```

## 設定オプション

### logLevel

ログ出力のレベルを指定します。

```json
{
  "logLevel": "info"
}
```

利用可能な値: `"trace"`, `"debug"`, `"info"`, `"warn"`, `"error"`, `"fatal"`

### locale

UIの言語を指定します。

```json
{
  "locale": "ja"
}
```

利用可能な値: `"ja"` (日本語), `"en"` (英語)

### theme

UIのテーマを指定します。

```json
{
  "theme": {
    "primaryColor": "cyan"
  }
}
```

利用可能な primaryColor: `"green"`, `"cyan"`, `"magenta"`, `"yellow"`, `"blue"`, `"red"`, `"white"`

### ui

UIの動作をカスタマイズします。

```json
{
  "ui": {
    "bookmarkIcon": "♥"
  }
}
```

#### ui.bookmarkIcon

リスト内でブックマークされたアクションの横に表示されるアイコンです。

デフォルト: `"♥"`

使用例: `"🔖"`, `"⭐"`, `"★"`, `"♥"`, `"📌"`

#### ui.actionListFormat

VSCodeスタイルのテンプレート構文を使用して、アクションリストの表示形式をカスタマイズします。

```json
{
  "ui": {
    "actionListFormat": "${selectIcon:width(2)}[${category:max}] ${name:max} ${description:grow} ${tags:max:dim:right} ${bookmark:width(2)}"
  }
}
```

**利用可能な変数:**

| 変数 | 説明 | 例 |
|------|------|-----|
| `${selectIcon}` | 選択インジケータ | `❯` or ` ` |
| `${bookmark}` | ブックマークアイコン | `♥` or ` ` |
| `${category}` | カテゴリラベル | `git`, `Project` |
| `${name}` | アクション名 | `my-action` |
| `${description}` | 説明 | `何かをする` |
| `${tags}` | タグ（#付き） | `#git #dev` |
| `${plugin}` | プラグイン名 | `arere-plugin-git` |
| `${source}` | ソース | `project`, `global`, `plugin` |

**利用可能な修飾子:**

| 修飾子 | カテゴリ | 説明 | 例 |
|--------|----------|------|-----|
| `:max` | 幅 | 同種変数の最大幅に揃える | `${name:max}` |
| `:width(N)` | 幅 | N文字の固定幅（超過時は`…`で切り詰め） | `${bookmark:width(2)}` |
| `:left` | 揃え | 左揃え（デフォルト、省略可） | `${name:max:left}` |
| `:right` | 揃え | 右揃え（パディングを左に） | `${tags:max:right}` |
| `:center` | 揃え | 中央揃え | `${name:max:center}` |
| `:grow` | 伸縮 | 残りスペースを埋める（flexGrow） | `${description:grow}` |
| `:shrink` | 伸縮 | 幅が足りない時に縮む | `${description:shrink}` |
| `:dim` | 色 | 非選択時に薄く表示 | `${tags:dim}` |
| `:primary` | 色 | プライマリカラーで表示 | `${name:primary}` |

**テキスト揃えの例:**

```
:right (右揃え)                :left (左揃え、デフォルト)     :center (中央揃え)
┌──────────┐                   ┌──────────┐                   ┌──────────┐
│1234567890│                   │1234567890│                   │1234567890│
│     12345│                   │12345     │                   │  12345   │
└──────────┘                   └──────────┘                   └──────────┘
  パディングが左                 パディングが右                 両側にパディング
```

**フォーマット例:**

ミニマル:
```json
{
  "ui": {
    "actionListFormat": "${selectIcon}${name}"
  }
}
```

カテゴリなし:
```json
{
  "ui": {
    "actionListFormat": "${selectIcon:width(2)}${name:max} ${description:grow} ${bookmark:width(2)}"
  }
}
```

数字を右揃え:
```json
{
  "ui": {
    "actionListFormat": "${index:width(5):right}| ${name:max}"
  }
}
```

ブックマークを左に:
```json
{
  "ui": {
    "actionListFormat": "${selectIcon:width(2)}${bookmark:width(2)} ${name:max} - ${description:grow}"
  }
}
```

### bookmarks

ブックマークされたアクションIDの配列です。Tabキーでブックマークをトグルすると自動的に管理されます。

```json
{
  "bookmarks": [
    "local:my-action",
    "arere-plugin-git:clone-repo"
  ]
}
```

形式: `{source}:{actionName}`
- `local:actionName` - ローカルアクション（プロジェクトまたはグローバル）
- `pluginName:actionName` - プラグインのアクション

### plugins

使用するプラグインを指定します。

```json
{
  "plugins": {
    "arere-plugin-git": true,
    "arere-plugin-docker": {
      "enabled": true,
      "config": {
        "defaultRegistry": "docker.io"
      }
    },
    "arere-plugin-aws": false
  }
}
```

### actionsDir

アクションを配置するディレクトリを指定します。

```json
{
  "actionsDir": "./sys-actions"
}
```

デフォルト: `.arere`

## マージ動作

settings.json は **深いマージ** を行います。ワークスペース設定で指定した項目のみが上書きされます。

**ユーザー設定** (`~/.arere/settings.json`):
```json
{
  "logLevel": "debug",
  "locale": "ja",
  "plugins": {
    "arere-plugin-git": true
  }
}
```

**ワークスペース設定** (`.arere/settings.json`):
```json
{
  "logLevel": "info",
  "plugins": {
    "arere-plugin-docker": true
  }
}
```

**結果**:
```json
{
  "logLevel": "info",           // ワークスペースで上書き
  "locale": "ja",               // ユーザー設定から継承
  "plugins": {
    "arere-plugin-git": true,   // ユーザー設定から継承
    "arere-plugin-docker": true // ワークスペースで追加
  }
}
```
