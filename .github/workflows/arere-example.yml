name: Arere Action Test

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

jobs:
  test-arere-action:
    name: Test arere-action
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Run ci-api-test with arere-action
        uses: ./actions/arere-action
        with:
          action: ci-api-test
